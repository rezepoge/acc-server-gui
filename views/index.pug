extends layout

block content
  h1= title

  main
    include serverSettings
    include eventOptions
    include assistRules

  a#results(href='/results') Results

  include buttons
  script(src='/javascripts/main.min.js?ver=' + version defer='defer')
